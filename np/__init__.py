# empty file, for package purposes
# __init__.py
# Only import functions that contain their only executable that is initiated upon opening
# The call to those executables will occur before any other import statements are executed here

import np.npmake